\section{Постановка задачи}

На плоскости заданно множество точек. Выбрать из них такие три точки, не лежащие на одной прямой, которые составляют треугольник наименьшей площади.

\begin{figure}[ht] 
	\centering 
	\includegraphics{pict} 
	\caption{ 
		\textbf{Множество из шести точек, где точки A, B и C-искомые, образуют треугольник наименьшей площади.} 
	} 
	%\label{fig_img11}
\end{figure}

\section{Уточнение исходных и выходных данных и ограничений на них}


\subsection{Исходные данные}
Будем решать задачу в системе координат. С клавиатуры на вход подаётся число $\mathbf{n}$ типа $int$, количество данных точек($\mathbf{n\geqslant 3}$). Также введем переменную $\mathbf{min}$, которой будет присваиваться наименьшее значение площади. Для каждого из $\mathbf{n}$ экземпляров класса $\mathbf{Point}$ с клавиатуры считываются (или случайным образом. пользователь может выбрать вариант: вводить координаты самому с клавиатуры или предоставить компьютеру выбрать их случайным образом, для этого будет создана переменная типа $boolean$) значения переменных $\mathbf{x_n}$ и $\mathbf{y_n}$, координаты точек на плоскости.
\subsection{Выходные данные}
Необходимо вывести на экран значение площади для треугольника, имеющего её минимальную. Помимо этого на рисунке будут выделены те три точки, которые образуют этот треугольник. При желании пользователь может вывести координаты этих точек в файл (output.txt, он будет лежать в одной папке с программой) нажатием кнопки.
\section{Выбор метода решения}


\subsection{Базовые структуры данных}
Класс $\mathbf{Point}$ описывает точку, состоит из двух полей $\mathbf{x_n}$ и $\mathbf{y_n}$ типа $double$, задающих координаты точки на плоскости.


Класс $\mathbf{Set}$ описывает множество точек, состоит из двух полей: $\mathbf{k}$ типа $int$(в данной задаче $\mathbf{k}$ всегда равна 3, так как три точки образуют треугольник) и массив состоящий из $\mathbf{k}$ экземпляров класса $\mathbf{Point}$.
\subsection{Описание алгоритма}
Будем решать задачу в системе координат. 
Создадим $\mathbf{C_n^3}$ $\mathbf{(C_n^3 = \frac{n!}{3! * (\, n-3 )\, !})}$ экземпляров класса $\mathbf{Set}$, состоящих из 3-х точек($\mathbf{Point}$).
С помощью метода $\mathit{square}$ получим значение площади для каждого из цэ треугольников, т.е. для каждого из $\mathbf{Set}$'ов. Опишем метод: будем считать 3 расстояния для каждого экземпляра: от точки $\mathbf{P_a}$ до точки $\mathbf{P_b}$, от точки $\mathbf{P_b}$ до точки $\mathbf{P_c}$ и от точки $\mathbf{P_c}$ до точки $\mathbf{P_a}$. Получив данные значения длины трех сторон по формуле $ L_a=\sqrt{(x_b-x_a)^2 + (y_b-y_a)^2}$-расстояние от $\mathbf{P_a}$ до $\mathbf{P_b}$(далее аналогично), посчитаем значение площади треугольника по формуле Герона: $ S = \sqrt{p(p-L_a)(p-L_b)(p-L_с)} $, где $p=(L_a+L_b+L_c)/2$. Будем каждый раз сравнивать значение площади $\mathbf{Set.square}$ со значением переменной $\mathbf{min}$ (с самого начала присвоим $\mathbf{min}$ значение площади первого $\mathbf{Set}$'a), и если новое значение меньше, то будем присваивать его переменной $\mathbf{min}$. Проверив все $\mathbf{C_n^3}$ вариантов получим конечное значение $\mathbf{min}$. 3 точки, образующие треугольник, соответствующий данному значению переменной $\mathbf{min}$, и будут искомыми.
Ответом на данную задачу является значение минимальной площади. При этом на плоскости будут отображены те три точки, которые образуют треугольник наименьшей площади.

Примечания: Для каждого $\mathbf{Set}$'а
площадь не должна быть нулевой, иначе данный $\mathbf{Set}$ противоречит условию(3 токчи лежат на одной пряой). То есть при создании $\mathbf{Set}$'а, если его площадь равно 0, то значение переменной $\mathbf{min}$ не меняется, а именно не становится нулевым.
\newpage
\section{Построение алгоритма}

\subsection{Обобщенная блок-схема алгоритма}

\begin{figure}[ht] 
	\centering 
	\includegraphics{obs} 
	
	%\label{fig_img11}
\end{figure}
\newpage


\subsection{Блок-схема алгоритма}
\begin{figure}[ht] 
	\centering 
	\includegraphics{bs} 	%\label{fig_img11}
\end{figure}
\newpage

\section{Листинг программы}

 \textbf{ double min = -1; } // значение минимальной площади

 \textbf{ MySet answertriangle = new MySet(); } //создадим треугольник минимальной площади

 \textbf{ Point[] points2 = new Point[points.size()]; }

 \textbf{ int n = points.size(); }

 \textbf{ for (int i = 0; i < n; i++) {points2[i] = points.get(i);} }

 \textbf{for (int i = 0; i < n; i++) }$\mathbf{ { }$ // переберем первую вершину треугольника

 \textbf{for (int j = 0; j < n; j++) }$\mathbf{ { }$   // переберем вторую вершину треугольника

 \textbf{for (int h = 0; h < n; h++) }$\mathbf{ { }$  // переберем третью вершину треугольника

 \textbf{if (i == j || i == h || j == h) continue; } // проверим, что мы выбрали три различные точки
			
 \textbf{MySet triangle = new MySet(3, points2[i], points2[j], points2[h]); } // создадим треугольник 

с данными тремя вершинами

 \textbf{double square = triangle.Square(); } // найдем его площадь

 \textbf{if (square > 0 'and' (min == -1 || min > square)) }$\mathbf{ { }$ // если треугольник не вырожден
	
-ный(площадь больше нуля) и его площадь меньше, чем та что была
				
 \textbf{min = square; } // обновим значение минимальной площади
				
 \textbf{answertriangle = triangle.myCopy(); }


\section{Пример работы программы}

\subsection{Исходные данные}
123 123

123 456

56 74

156 90

125 76

65 13

145 200

231 345

461 398

423 358

10 53
\subsection{Выходные данные}
198.0

123 456

156 90

145 200
\section{Анализ правильности решения}
Чтобы убедиться в праильности решения выберем четыре точки, для которых будет очевидно нахождение треугольника наименьшей площади, и посчитаем это значение. Затем убедимся в том, что программа выдает те же значения.

Создадим вручную три точки: 


\begin{figure}[H] 
	\centering 
	\includegraphics{1} 	%\label{fig_img11}
\end{figure}


первая точка с координатами $\mathbf{(1;1)}$,


\begin{figure}[H] 
	\centering 
	\includegraphics{2} 	%\label{fig_img11}
\end{figure}


вторая точка с координатами $\mathbf{(5;1)}$,


\begin{figure}[H] 
	\centering 
	\includegraphics{3} 	%\label{fig_img11}
\end{figure}



третья точка с координатами $\mathbf{(1;5)}$


\begin{figure}[H] 
	\centering 
	\includegraphics{4} 	%\label{fig_img11}
\end{figure}


и четвёртая точка с координатами $\mathbf{(123;456)}$.


Очевидно, что наименьшую площадь образует треугольник, заданный первыми тремя точками. Посчитаем его площадь.
Первая сторона равна:\mathbf{ $\sqrt{(5-1)^2 + (1-1)^2}=4$}, вторая сторона: \mathbf{$\sqrt{(1-5)^2 + (5-1)^2}=4\sqrt{2}$} и третья сторона: \mathbf{$\sqrt{(1-1)^2 + (5-1)^2}=4$}. Теперь посчитаем площадь(облегчили вычисления тем, что треугольник прямоугольный). Площадь равна: \mathbf{$\frac{(4*4)}{2}=8$}.

\begin{figure}[H] 
	\centering 
	\includegraphics{5} 	%\label{fig_img11}
\end{figure}


Запустив код, убедимся в том, что треугольник выбран правильно и площадь посчитана верно.


\begin{figure}[H] 
	\centering 
	\includegraphics{6} 	%\label{fig_img11}
\end{figure}


Теперь проверим правильность выбранных точек, а именно, проверим, что будет при выводе в файл(там толжны быть отображены координаты тех трех точек, которые образуют треуггольник наименьшей площади).


Таким образом на простом примере мы убедились в корректной работе описываемой выше программы.